name: try_workflow #name of workflow

on: 
  push:
    branches: [main]

jobs:
  build:           #jobid
    runs-on: ubuntu-latest #runner for the job

    steps:
      - name: checkout code
        uses: actions/checkout@v6
